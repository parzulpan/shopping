spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # nacos 注册中心的服务地址
  application:
    name: shopping-auth-server # 当前服务的名字
  thymeleaf:
    cache: false
server:
  port: 20000

# 开启 SQL 调试
logging:
  level:
    cn.parzulpan.shopping: debug